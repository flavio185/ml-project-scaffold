{
  "job": [
    {
      "name": "job1",
      "config": {
        "min_replicas": 1,
        "max_replicas": 5,
        "entry_point": "uv run {{ cookiecutter.model_name }}/run.py",
        "namespace": "app_ns"
      }
    }
  ]
}